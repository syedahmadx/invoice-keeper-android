<resources>
    <!-- Colours live in Compose (ui/theme/Color.kt). This XML theme only needs to give the
         window a neutral, action-bar-free base before Compose takes over. -->
    <style name="Theme.InvoiceKeeper" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
    </style>
</resources>
